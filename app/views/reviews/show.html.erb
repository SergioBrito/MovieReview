<h1>Review for <%= @movie.name %></h1>

<p>
  <strong>Reviewer:</strong>
  <%= @review.reviewer_name.present? ? @review.reviewer_name : "Anonymous" %>
</p>

<h3>Ratings:</h3>
<ul>
  <% @review.review_ratings.each do |rating| %>
    <li>
      <strong><%= rating.category.name %>:</strong> <%= rating.score %>
    </li>
  <% end %>
</ul>

<p>
  <strong>Total Score:</strong> <%= @review.total_score %>
</p>

<%= link_to "Back to Reviews", movie_reviews_path(@movie) %> |
<%= link_to "Back to Movie", movie_path(@movie) %>
